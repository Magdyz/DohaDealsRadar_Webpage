# Supabase Configuration
# Get these from: Supabase Dashboard > Project Settings > API
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# Supabase Service Role Key (PRIVATE - Server-side only)
# This key bypasses RLS and should NEVER be exposed to the client
# Used in API routes for admin operations
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# API Configuration (use /api for Next.js API routes)
NEXT_PUBLIC_API_BASE_URL=/api

# Authentication is handled by Supabase Auth (OTP via email)
# Configure email settings in: Supabase Dashboard > Authentication > Email Templates
